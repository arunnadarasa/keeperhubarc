---
phase: 29-build-evaluate-command-and-calibration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/get-shit-done/workflows/plan-phase.md
autonomous: true
requirements:
  - CMD-04

must_haves:
  truths:
    - "gsd-planner knows it must author EVAL-CONFIG.yml for phases with runtime-verifiable success criteria"
    - "The requirement to write EVAL-CONFIG.yml alongside PLAN.md files is explicit text in plan-phase.md, not just implied"
    - "plan-phase.md still works identically for phases that do not need evaluation (full backward compatibility)"
  artifacts:
    - path: "~/.claude/get-shit-done/workflows/plan-phase.md"
      provides: "EVAL-CONFIG.yml authoring instruction in planner prompt"
      contains: "EVAL-CONFIG.yml"
  key_links:
    - from: "plan-phase.md Step 8 planner prompt"
      to: "EVAL-CONFIG.yml authoring note"
      via: "text note in quality_gate or planner prompt body"
      pattern: "EVAL-CONFIG"
---

<objective>
Add an explicit EVAL-CONFIG.yml authoring note to `~/.claude/get-shit-done/workflows/plan-phase.md` so that gsd-planner knows to author `{padded-phase}-EVAL-CONFIG.yml` alongside PLAN.md files for phases with runtime-verifiable success criteria.

Purpose: CMD-04 is not a new feature — `/gsd:new-milestone` already works and is the standard flow. The deliverable is making the EVAL-CONFIG.yml authoring expectation explicit in the planner's prompt, so it becomes visible as a required step rather than something implied from context. This is a 2-3 line documentation addition, not a structural change.

Output: `~/.claude/get-shit-done/workflows/plan-phase.md` with EVAL-CONFIG.yml authoring note added in Step 8's planner prompt section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-evaluate-command-and-calibration/29-CONTEXT.md
@.planning/phases/29-build-evaluate-command-and-calibration/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EVAL-CONFIG.yml authoring note to plan-phase.md</name>
  <files>~/.claude/get-shit-done/workflows/plan-phase.md</files>

  <read_first>
    - ~/.claude/get-shit-done/workflows/plan-phase.md — full file, specifically Step 8 (Spawn gsd-planner Agent) and the quality_gate block inside the planner prompt (lines ~551-561)
    - .planning/phases/25-loop-architecture-and-evaluator-agent/25-EVAL-CONFIG-SPEC.md — EVAL-CONFIG.yml schema (so the note references correct field names)
    - .planning/phases/29-build-evaluate-command-and-calibration/29-RESEARCH.md — CMD-04 section confirming exact target location and wording guidance
  </read_first>

  <action>
In `~/.claude/get-shit-done/workflows/plan-phase.md`, locate the `<quality_gate>` block inside the planner prompt (inside the Step 8 markdown code fence, around lines 551-561). This block currently ends with:

```markdown
- [ ] must_haves derived from phase goal
```

Add the following item at the end of the quality_gate checklist (after the `must_haves` line):

```markdown
- [ ] For phases with runtime-verifiable success criteria: `{padded-phase}-EVAL-CONFIG.yml` authored alongside PLAN.md files with `threshold`, `max_rounds`, `server_port`, `seed_scripts`, and `criteria` fields per the EVAL-CONFIG.yml spec
```

No other changes to plan-phase.md. Do not restructure steps, do not add new sections, do not modify existing text.

**Why this location:** The `<quality_gate>` block inside the planner prompt is the canonical checklist the gsd-planner agent uses to verify its own output before returning. Adding EVAL-CONFIG.yml here makes it a first-class checklist item, not a buried note. The planner sees this checklist on every run and will know to check whether EVAL-CONFIG.yml should be written for the current phase.

**Exact insertion:** The new line goes immediately after `- [ ] must_haves derived from phase goal` and before the closing `</quality_gate>` tag.
  </action>

  <verify>
    <automated>grep -q "EVAL-CONFIG.yml" ~/.claude/get-shit-done/workflows/plan-phase.md && echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "EVAL-CONFIG.yml" ~/.claude/get-shit-done/workflows/plan-phase.md` returns at least 1
    - The new line contains the phrase "runtime-verifiable success criteria"
    - The new line is inside the `<quality_gate>` block within the Step 8 planner prompt section
    - The new line follows the `- [ ] must_haves derived from phase goal` line
    - All existing quality_gate items are preserved unchanged (no deletions)
    - `wc -l ~/.claude/get-shit-done/workflows/plan-phase.md` returns the original line count plus 1 (exactly one line added)
  </acceptance_criteria>

  <done>
    plan-phase.md contains an EVAL-CONFIG.yml authoring checklist item in the Step 8 quality_gate block. gsd-planner will see and apply this requirement on every plan-phase run for phases with runtime-verifiable criteria.
  </done>
</task>

</tasks>

<verification>
```bash
# Verify EVAL-CONFIG.yml note is present in plan-phase.md
grep -n "EVAL-CONFIG.yml" ~/.claude/get-shit-done/workflows/plan-phase.md

# Verify quality_gate block is intact (should still have all original items)
grep -c "^\- \[ \]" ~/.claude/get-shit-done/workflows/plan-phase.md
```
</verification>

<success_criteria>
- plan-phase.md contains EVAL-CONFIG.yml authoring note in the quality_gate block inside Step 8's planner prompt
- CMD-04: The standard development process now explicitly includes EVAL-CONFIG.yml authoring at plan time
- Full backward compatibility: phases without runtime-verifiable criteria are unaffected (note says "For phases with runtime-verifiable success criteria")
</success_criteria>

<output>
After completion, create `.planning/phases/29-build-evaluate-command-and-calibration/29-02-SUMMARY.md`
</output>
