---
phase: 29-build-evaluate-command-and-calibration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/calibration/fixtures/01-clean-pass/EVAL-CONFIG.yml
  - .planning/calibration/fixtures/01-clean-pass/expected-EVAL.md
  - .planning/calibration/fixtures/01-clean-pass/README.md
  - .planning/calibration/fixtures/02-clean-fail/EVAL-CONFIG.yml
  - .planning/calibration/fixtures/02-clean-fail/expected-EVAL.md
  - .planning/calibration/fixtures/02-clean-fail/README.md
  - .planning/calibration/fixtures/03-convergence-halt/EVAL-CONFIG.yml
  - .planning/calibration/fixtures/03-convergence-halt/expected-EVAL.md
  - .planning/calibration/fixtures/03-convergence-halt/README.md
  - .planning/calibration/fixtures/04-threshold-boundary/EVAL-CONFIG.yml
  - .planning/calibration/fixtures/04-threshold-boundary/expected-EVAL.md
  - .planning/calibration/fixtures/04-threshold-boundary/README.md
  - .planning/calibration/fixtures/05-seed-failure/EVAL-CONFIG.yml
  - .planning/calibration/fixtures/05-seed-failure/expected-EVAL.md
  - .planning/calibration/fixtures/05-seed-failure/README.md
autonomous: true
requirements:
  - CMD-01

must_haves:
  truths:
    - "5 calibration fixture pairs exist at .planning/calibration/fixtures/"
    - "Each fixture has EVAL-CONFIG.yml (input), expected-EVAL.md (golden output), and README.md"
    - "Fixture EVAL-CONFIG.yml files use synthetic paths (not real test file paths) clearly marked as calibration-only"
    - "expected-EVAL.md files have valid YAML frontmatter matching the EVAL.md format spec (all required fields)"
    - "The 5 fixtures cover: clean pass, clean fail, convergence halt, threshold boundary, and seed failure scenarios"
    - "approved field in expected-EVAL.md frontmatter is a YAML boolean (true or false, not quoted string)"
  artifacts:
    - path: ".planning/calibration/fixtures/01-clean-pass/expected-EVAL.md"
      provides: "golden EVAL.md for happy path"
      contains: "approved: true"
    - path: ".planning/calibration/fixtures/02-clean-fail/expected-EVAL.md"
      provides: "golden EVAL.md for clean failure"
      contains: "approved: false"
    - path: ".planning/calibration/fixtures/03-convergence-halt/expected-EVAL.md"
      provides: "golden EVAL.md for convergence halt scenario"
      contains: "status: convergence_halt"
    - path: ".planning/calibration/fixtures/04-threshold-boundary/expected-EVAL.md"
      provides: "golden EVAL.md for threshold boundary"
      contains: "score:"
    - path: ".planning/calibration/fixtures/05-seed-failure/expected-EVAL.md"
      provides: "golden EVAL.md for seed failure scenario"
      contains: "seed_ran: false"
  key_links:
    - from: ".planning/calibration/fixtures/"
      to: "EVAL-FORMAT-SPEC.md"
      via: "frontmatter schema compliance"
      pattern: "phase:|evaluated:|status:|score:|round:|max_rounds:|approved:|server_port:|seed_ran:|gaps:"
    - from: ".planning/calibration/fixtures/"
      to: "EVAL-CONFIG-SPEC.md"
      via: "EVAL-CONFIG.yml schema compliance"
      pattern: "threshold:|max_rounds:|server_port:|seed_scripts:|criteria:"
---

<objective>
Create 5 calibration fixture pairs in `.planning/calibration/fixtures/`. Each fixture is a directory containing `EVAL-CONFIG.yml` (input), `expected-EVAL.md` (golden output with known verdict), and `README.md` (2-3 lines describing what the fixture tests).

Purpose: These are documentation artifacts for evaluator regression testing — used to verify evaluator behavior after prompt changes. They are NOT runnable test infrastructure. They lock in known correct pass/fail verdicts for five distinct behavioral scenarios.

Output: 15 files across 5 fixture directories under `.planning/calibration/fixtures/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-build-evaluate-command-and-calibration/29-CONTEXT.md
@.planning/phases/29-build-evaluate-command-and-calibration/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create calibration fixture directory and all 5 fixture pairs</name>
  <files>
    .planning/calibration/fixtures/01-clean-pass/EVAL-CONFIG.yml
    .planning/calibration/fixtures/01-clean-pass/expected-EVAL.md
    .planning/calibration/fixtures/01-clean-pass/README.md
    .planning/calibration/fixtures/02-clean-fail/EVAL-CONFIG.yml
    .planning/calibration/fixtures/02-clean-fail/expected-EVAL.md
    .planning/calibration/fixtures/02-clean-fail/README.md
    .planning/calibration/fixtures/03-convergence-halt/EVAL-CONFIG.yml
    .planning/calibration/fixtures/03-convergence-halt/expected-EVAL.md
    .planning/calibration/fixtures/03-convergence-halt/README.md
    .planning/calibration/fixtures/04-threshold-boundary/EVAL-CONFIG.yml
    .planning/calibration/fixtures/04-threshold-boundary/expected-EVAL.md
    .planning/calibration/fixtures/04-threshold-boundary/README.md
    .planning/calibration/fixtures/05-seed-failure/EVAL-CONFIG.yml
    .planning/calibration/fixtures/05-seed-failure/expected-EVAL.md
    .planning/calibration/fixtures/05-seed-failure/README.md
  </files>

  <read_first>
    - .planning/phases/25-loop-architecture-and-evaluator-agent/25-EVAL-FORMAT-SPEC.md — EVAL.md YAML frontmatter schema (all required fields, approved boolean rules, status values)
    - .planning/phases/25-loop-architecture-and-evaluator-agent/25-EVAL-CONFIG-SPEC.md — EVAL-CONFIG.yml schema (threshold, max_rounds, server_port, seed_scripts, criteria types)
    - .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml — working reference implementation for all criterion types
    - .planning/phases/29-build-evaluate-command-and-calibration/29-RESEARCH.md — fixture scenarios table and anti-patterns (especially: use synthetic paths, mark as calibration-only)
  </read_first>

  <action>
Create all 15 files using the Write tool. All paths are relative to the repo root. Create parent directories as needed.

CRITICAL: All EVAL-CONFIG.yml criterion `playwright_test` and `test_file` paths MUST be synthetic (e.g., `tests/evaluate/fixtures/hypothetical.test.ts`) — NOT real paths from `tests/e2e/`. Each EVAL-CONFIG.yml MUST include a comment: `# CALIBRATION ONLY -- not runnable. Synthetic paths for evaluator regression testing.`

CRITICAL: The `approved:` field in all expected-EVAL.md YAML frontmatter MUST be a bare YAML boolean (`true` or `false`), NOT a quoted string (`"true"` or `"false"`).

The 5 fixture scenarios to implement:

**Fixture 01 — 01-clean-pass: Happy path, all criteria pass, approved: true**

README.md content: Happy path fixture. All 3 autonomous criteria pass. Tests that evaluator correctly writes approved: true when threshold is satisfied. Not runnable -- synthetic test paths.

EVAL-CONFIG.yml top-level fields: threshold 0.85, max_rounds 3, server_port 3099. seed_scripts: [scripts/evaluate/fixtures/hypothetical-seed.ts]. Three criteria: API-01 (api_assertion: GET /api/health, method GET, url /api/health, body null, auth none, expected_status 200, expected_body_contains ok), UI-01 (ui_behavior: Dashboard renders for authenticated user, playwright_test tests/evaluate/fixtures/hypothetical.test.ts, grep_pattern "dashboard renders @autonomous"), DS-01 (design_token: No hardcoded colors in hypothetical component, token_audit true, files: [components/hypothetical/hypothetical.tsx]).

expected-EVAL.md YAML frontmatter: phase "fixture-01-clean-pass", evaluated "2026-03-29T00:00:00Z", status passed, score "3/3", round 1, max_rounds 3, approved true, server_port 3099, seed_ran true, gaps []. Markdown body: heading "# Evaluation Report: Fixture 01 -- clean-pass", summary line "Round: 1 of 3 / Score: 3/3 / Status: PASSED / Status: APPROVED", criteria table showing API-01 PASS, UI-01 PASS, DS-01 PASS.

**Fixture 02 — 02-clean-fail: All criteria fail, approved: false, gaps populated**

README.md content: All-fail fixture. All 3 autonomous criteria fail. Tests that evaluator correctly writes approved: false and populates gaps array. Not runnable -- synthetic test paths.

EVAL-CONFIG.yml top-level fields: threshold 0.85, max_rounds 3, server_port 3099. seed_scripts: [scripts/evaluate/fixtures/hypothetical-seed.ts]. Three criteria: API-01 (api_assertion: GET /api/hypothetical returns 201 with id field, method GET, url /api/hypothetical, body null, auth test-user, expected_status 201, expected_body_contains id), UI-01 (ui_behavior: Hypothetical modal renders after button click, playwright_test tests/evaluate/fixtures/hypothetical.test.ts, grep_pattern "modal renders @autonomous"), DS-01 (design_token: No hardcoded colors in hypothetical modal, token_audit true, files: [components/hypothetical/modal.tsx]).

expected-EVAL.md YAML frontmatter: phase "fixture-02-clean-fail", evaluated "2026-03-29T00:00:00Z", status failed, score "0/3", round 1, max_rounds 3, approved false, server_port 3099, seed_ran true. gaps array: (1) criterion "API-01: GET /api/hypothetical returns 201", type api_assertion, status failed, reason "Endpoint returned HTTP 404 -- route does not exist", evidence "HTTP/1.1 404 Not Found", fix_hints ["Create GET /api/hypothetical route returning 201 with {id: string}"]. (2) criterion "UI-01: Hypothetical modal renders", type ui_behavior, status failed, reason "Modal element not found after click", evidence "Error: locator not found after 5000ms", fix_hints ["Add data-testid=hypothetical-modal to Modal component"]. (3) criterion "DS-01: No hardcoded colors", type design_token, status failed, reason "token-audit.js found hardcoded color", evidence "modal.tsx:12: hardcoded color #1a1a2e", fix_hints ["Replace #1a1a2e with var(--color-background)"]. Markdown body: heading, summary "FAILED -- 3 gaps blocking threshold / Status: REJECTED", criteria table showing all FAIL, then Gaps section with Gap 1, Gap 2, Gap 3 details.

**Fixture 03 — 03-convergence-halt: Round 2 identical failures, status: convergence_halt**

README.md content: Convergence halt fixture. Round 2 failures are identical to round 1. Tests that evaluator writes status: convergence_halt and approved: false without consuming another round. Not runnable -- synthetic test paths.

EVAL-CONFIG.yml top-level fields: threshold 0.85, max_rounds 3, server_port 3099. seed_scripts: [scripts/evaluate/fixtures/hypothetical-seed.ts]. Two criteria: API-01 (api_assertion: GET /api/hypothetical returns 200, method GET, url /api/hypothetical, body null, auth none, expected_status 200), UI-01 (ui_behavior: Hypothetical page renders title, playwright_test tests/evaluate/fixtures/hypothetical.test.ts, grep_pattern "renders title @autonomous").

expected-EVAL.md represents the ROUND 2 output (the convergence halt). YAML frontmatter: phase "fixture-03-convergence-halt", evaluated "2026-03-29T00:00:00Z", status convergence_halt, score "0/2", round 2, max_rounds 3, approved false, server_port 3099, seed_ran true. gaps array: (1) criterion "API-01: GET /api/hypothetical returns 200", type api_assertion, status failed, reason "Endpoint returned HTTP 404 for the second consecutive round", evidence "HTTP/1.1 404 Not Found", fix_hints ["Route still missing after gap-fix round -- escalate to human review"]. (2) criterion "UI-01: Hypothetical page renders title", type ui_behavior, status failed, reason "Page title not found for second consecutive round", evidence "Error: h1 not found", fix_hints ["Component still missing after gap-fix round -- escalate"]. delta: fixed [], still_failing [{criterion: "API-01", evidence: "HTTP 404 -- unchanged from round 1"}, {criterion: "UI-01", evidence: "Playwright exit 1 -- unchanged from round 1"}], new_failures []. Markdown body: heading for round 2, summary "CONVERGENCE HALT -- same failures as round 1 / Status: REJECTED", criteria table with both FAIL, Gaps section, Delta section showing Still failing, no Fixed items.

**Fixture 04 — 04-threshold-boundary: 2/3 pass, threshold 0.67, approved: false**

README.md content: Threshold boundary fixture. 2 of 3 autonomous criteria pass with threshold 0.67. Tests threshold math: 2/3 = 0.666 which is below 0.67, so approved: false. A threshold of 0.66 would pass. Not runnable -- synthetic test paths.

EVAL-CONFIG.yml top-level fields: threshold 0.67 (not 0.85 -- this is the boundary test), max_rounds 3, server_port 3099. Include comment: "# threshold: 0.67 -- boundary test: 2/3 = 0.666 < 0.67, so approved: false. threshold: 0.66 would pass". seed_scripts: [scripts/evaluate/fixtures/hypothetical-seed.ts]. Three criteria: API-01 (api_assertion: GET /api/health returns 200, method GET, url /api/health, body null, auth none, expected_status 200, expected_body_contains ok), API-02 (api_assertion: GET /api/hypothetical returns 200, method GET, url /api/hypothetical, body null, auth test-user, expected_status 200), UI-01 (ui_behavior: Hypothetical feature renders correctly, playwright_test tests/evaluate/fixtures/hypothetical.test.ts, grep_pattern "feature renders @autonomous").

expected-EVAL.md YAML frontmatter: phase "fixture-04-threshold-boundary", evaluated "2026-03-29T00:00:00Z", status failed, score "2/3", round 1, max_rounds 3, approved false, server_port 3099, seed_ran true. gaps array: (1) criterion "UI-01: Hypothetical feature renders correctly", type ui_behavior, status failed, reason "Playwright test failed -- feature component not found", evidence "Error: locator not found", fix_hints ["Add data-testid=hypothetical-feature to component"]. Markdown body: heading, summary "FAILED -- 1 gap blocking threshold (2/3 = 0.666, below threshold 0.67) / Status: REJECTED", criteria table showing API-01 PASS, API-02 PASS, UI-01 FAIL, then Gaps section.

**Fixture 05 — 05-seed-failure: Seed script exits non-zero, seed_ran: false**

README.md content: Seed failure fixture. Seed script exits non-zero. Tests that evaluator sets seed_ran: false in EVAL.md and continues to evaluate criteria (seeds are best-effort per spec). Not runnable -- synthetic test paths.

EVAL-CONFIG.yml top-level fields: threshold 0.85, max_rounds 3, server_port 3099. seed_scripts: [scripts/evaluate/fixtures/failing-seed.ts] (a synthetic seed that would exit non-zero). One criterion: API-01 (api_assertion: GET /api/health returns 200 even when seed fails, method GET, url /api/health, body null, auth none, expected_status 200).

expected-EVAL.md YAML frontmatter: phase "fixture-05-seed-failure", evaluated "2026-03-29T00:00:00Z", status failed, score "0/1", round 1, max_rounds 3, approved false, server_port 3099, seed_ran false (YAML boolean false). gaps array: (1) criterion "API-01: GET /api/health returns 200 even when seed fails", type api_assertion, status failed, reason "Health endpoint unreachable -- dev server may not have started correctly after seed failure", evidence "Connection refused: http://localhost:3099/api/health", fix_hints ["Check failing-seed.ts for root cause", "Ensure dev server startup is not dependent on seed script success"]. Markdown body: heading, summary "FAILED -- 1 gap blocking threshold / Seed: failed / Status: REJECTED", criteria table with API-01 FAIL, Gaps section.

Write all 15 files using the Write tool. Structure each EVAL-CONFIG.yml as valid YAML, and each expected-EVAL.md as markdown with YAML frontmatter (the frontmatter opens and closes with the YAML delimiter line).
  </action>

  <verify>
    <automated>
test -d .planning/calibration/fixtures/01-clean-pass && test -d .planning/calibration/fixtures/02-clean-fail && test -d .planning/calibration/fixtures/03-convergence-halt && test -d .planning/calibration/fixtures/04-threshold-boundary && test -d .planning/calibration/fixtures/05-seed-failure && grep -q "approved: true" .planning/calibration/fixtures/01-clean-pass/expected-EVAL.md && grep -q "approved: false" .planning/calibration/fixtures/02-clean-fail/expected-EVAL.md && grep -q "status: convergence_halt" .planning/calibration/fixtures/03-convergence-halt/expected-EVAL.md && grep -q "score: " .planning/calibration/fixtures/04-threshold-boundary/expected-EVAL.md && grep -q "seed_ran: false" .planning/calibration/fixtures/05-seed-failure/expected-EVAL.md && echo "PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/calibration/fixtures/` directory exists with 5 subdirectories
    - Each subdirectory contains exactly 3 files: EVAL-CONFIG.yml, expected-EVAL.md, README.md
    - `grep -q "CALIBRATION ONLY" .planning/calibration/fixtures/01-clean-pass/EVAL-CONFIG.yml` exits 0 (all 5 have this comment)
    - `grep -q "approved: true" .planning/calibration/fixtures/01-clean-pass/expected-EVAL.md` exits 0
    - `grep -q "approved: false" .planning/calibration/fixtures/02-clean-fail/expected-EVAL.md` exits 0
    - `grep -q "status: convergence_halt" .planning/calibration/fixtures/03-convergence-halt/expected-EVAL.md` exits 0
    - `grep -q "score: \"2/3\"" .planning/calibration/fixtures/04-threshold-boundary/expected-EVAL.md` exits 0
    - `grep -q "seed_ran: false" .planning/calibration/fixtures/05-seed-failure/expected-EVAL.md` exits 0
    - All 5 expected-EVAL.md files contain all required YAML frontmatter fields: phase, evaluated, status, score, round, max_rounds, approved, server_port, seed_ran, gaps
    - No fixture EVAL-CONFIG.yml references `tests/e2e/playwright/` paths
    - `grep -rq "tests/evaluate/fixtures" .planning/calibration/fixtures/` exits 0 (synthetic paths used)
    - `approved:` values in all expected-EVAL.md files are bare booleans (not quoted strings)
  </acceptance_criteria>

  <done>
    5 calibration fixture directories created at `.planning/calibration/fixtures/` with all 15 files. Each fixture pair covers a distinct behavioral scenario. EVAL-CONFIG.yml files use synthetic paths and are marked CALIBRATION ONLY. expected-EVAL.md files have valid YAML frontmatter with all required fields and known correct verdicts.
  </done>
</task>

</tasks>

<verification>
```bash
for f in 01-clean-pass 02-clean-fail 03-convergence-halt 04-threshold-boundary 05-seed-failure; do
  echo "--- ${f} ---"
  ls .planning/calibration/fixtures/${f}/
  grep "^approved:\|^status:\|^seed_ran:\|^score:" .planning/calibration/fixtures/${f}/expected-EVAL.md
done
grep -r "tests/e2e" .planning/calibration/fixtures/ && echo "ERROR: real paths found" || echo "PASS: no real paths"
```
</verification>

<success_criteria>
- 15 files exist across 5 fixture directories
- CMD-01 regression testing support: evaluators can be validated against 5 known scenarios
- All fixtures use synthetic paths marked as calibration-only
- approved boolean field is bare YAML boolean in all expected-EVAL.md files
- Scenarios covered: clean pass, clean fail, convergence halt, threshold boundary, seed failure
</success_criteria>

<output>
After completion, create `.planning/phases/29-build-evaluate-command-and-calibration/29-03-SUMMARY.md`
</output>
