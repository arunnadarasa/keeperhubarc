---
phase: 26-dev-server-lifecycle-and-evaluation-harness
plan: "03"
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - tests/e2e/playwright/eval-harness.test.ts
  - .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-05]

must_haves:
  truths:
    - "At least one @autonomous-tagged test exists that exercises API assertion (EVAL-01)"
    - "At least one @autonomous-tagged test exists that exercises UI behavior verification (EVAL-02)"
    - "26-EVAL-CONFIG.yml exists with valid schema matching EVAL-CONFIG-SPEC.md"
    - "Running --grep '@autonomous' with playwright.evaluate.config.ts finds the new tests"
    - "token-audit.js --quiet exit code behavior is documented in EVAL-CONFIG.yml criteria"
  artifacts:
    - path: "tests/e2e/playwright/eval-harness.test.ts"
      provides: "Smoke tests tagged @autonomous: one api_assertion test, one ui_behavior test"
      contains: "@autonomous"
    - path: ".planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml"
      provides: "Evaluation config for Phase 26 itself"
      contains: "threshold: 0.85"
  key_links:
    - from: ".planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml"
      to: "tests/e2e/playwright/eval-harness.test.ts"
      via: "playwright_test field in ui_behavior criteria"
      pattern: "eval-harness.test.ts"
    - from: ".planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml"
      to: "scripts/token-audit.js"
      via: "design_token criterion"
      pattern: "design_token"
---

<objective>
Create the evaluation test file with `@autonomous`-tagged tests (HTTP assertion + UI behavior) and write the `26-EVAL-CONFIG.yml` that declares criteria for evaluating Phase 26 itself.

Purpose: EVAL-01 and EVAL-02 require the harness to support HTTP assertions and Playwright UI tests. This plan creates the canonical first `@autonomous` tests establishing the tagging convention. EVAL-05 requires token-audit.js to run as a deterministic gate -- the EVAL-CONFIG.yml wires this as a design_token criterion. The EVAL-CONFIG.yml also ensures this phase can be evaluated by gsd-evaluator when it is eventually invoked.

Output:
- tests/e2e/playwright/eval-harness.test.ts (two @autonomous smoke tests)
- .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-RESEARCH.md
@.planning/phases/25-loop-architecture-and-evaluator-agent/25-EVAL-CONFIG-SPEC.md
@.planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-02-SUMMARY.md

<interfaces>
From tests/e2e/playwright/utils/auth.ts (auth helpers):
```typescript
export async function signIn(page: Page, email: string, password: string): Promise<void>
export async function signUpAndVerify(page: Page): Promise<{ email: string; password: string }>
```

From tests/e2e/playwright/utils/db.ts (persistent user credentials):
```typescript
export const PERSISTENT_TEST_USER_EMAIL = "pr-test-do-not-delete@techops.services";
export const PERSISTENT_TEST_PASSWORD = "TestPassword123!";
```

APIRequestContext usage in Playwright (for HTTP assertions):
```typescript
import { test, expect } from "@playwright/test";
// The `request` fixture in a project with storageState inherits the auth session.
// No manual auth header needed for authenticated API calls.
test("API returns 200 @autonomous", async ({ request }) => {
  const response = await request.get("/api/workflows");
  expect(response.status()).toBe(200);
});
```

EVAL-CONFIG criterion types from 25-EVAL-CONFIG-SPEC.md:
- api_assertion: method, url, body, auth, expected_status, expected_body_contains
- ui_behavior: playwright_test, grep_pattern
- design_token: token_audit: true, files: [...]
- unit_test: test_file, grep_pattern
- manual_review: review_guidance
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create eval-harness.test.ts with @autonomous smoke tests</name>
  <files>tests/e2e/playwright/eval-harness.test.ts</files>
  <read_first>
    - tests/e2e/playwright/playwright.evaluate.config.ts (understand baseURL and project setup)
    - tests/e2e/playwright/auth.setup.ts (understand what storageState provides)
    - tests/e2e/playwright/utils/db.ts (PERSISTENT_TEST_USER_EMAIL and PERSISTENT_TEST_PASSWORD)
    - tests/e2e/playwright/workflow.test.ts (study test structure patterns used in this codebase)
  </read_first>
  <action>
Create `tests/e2e/playwright/eval-harness.test.ts` with two `@autonomous`-tagged tests. This file establishes the `@autonomous` tagging convention for the evaluation harness.

Requirements for each test:
- Tag with `@autonomous` as a literal substring in the test name (Playwright --grep matches on this)
- Must work with the `chromium` project which has storageState from auth.setup.ts
- No retries -- these are deterministic, not flaky

Test 1: API assertion (EVAL-01)
- Name: "health endpoint returns 200 @autonomous"
- Uses `request` fixture to GET /api/health (or /api/og/health, whichever exists -- check by reading the existing test files to find a simple API endpoint that returns 200 without complex auth; if unsure, use GET /api/workflows which is a known authenticated endpoint returning 200)
- Asserts `response.status()` is 200
- NOTE: Check existing test files (auth.test.ts, workflow.test.ts) to find a suitable endpoint that reliably returns 200

Test 2: UI behavior (EVAL-02)
- Name: "dashboard renders for authenticated user @autonomous"
- Navigate to "/" (root, which redirects to dashboard for authenticated users)
- Assert the org switcher combobox is visible: `page.locator('button[role="combobox"]')`
- Use `waitUntil: "domcontentloaded"` on page.goto
- Timeout: 15_000ms on waitForSelector

Important: The `@autonomous` tag is embedded in the test name string. It is NOT a separate test tag, annotation, or metadata system. This is a Playwright --grep convention.

Use `test.describe` to group both tests under a describe block named "Evaluation Harness Smoke Tests".

Exact file to write:
```typescript
import { expect, test } from "@playwright/test";

test.describe("Evaluation Harness Smoke Tests", () => {
  test("health endpoint returns 200 @autonomous", async ({ request }) => {
    const response = await request.get("/api/health");
    expect(response.status()).toBe(200);
  });

  test("dashboard renders for authenticated user @autonomous", async ({
    page,
  }) => {
    await page.goto("/", { waitUntil: "domcontentloaded" });
    await page.waitForSelector('button[role="combobox"]', {
      state: "visible",
      timeout: 15_000,
    });
    await expect(page.locator('button[role="combobox"]')).toBeVisible();
  });
});
```

IMPORTANT: Before writing the API test, verify that `/api/health` returns 200 by checking if a health route exists in `app/api/health/route.ts`. If it does not exist, substitute with the first simple API endpoint you find in existing tests (workflow.test.ts or auth.test.ts commonly have GET /api/workflows or similar). Document which endpoint you used and why in a brief comment.

After writing, run the dry-run to confirm tests are discovered:
```bash
npx playwright test --config tests/e2e/playwright/playwright.evaluate.config.ts --grep "@autonomous" --dry-run
```
This should list both tests without starting the server.
  </action>
  <verify>
    <automated>npx playwright test --config tests/e2e/playwright/playwright.evaluate.config.ts --grep "@autonomous" --dry-run 2>&1 | grep -c "@autonomous"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "@autonomous" tests/e2e/playwright/eval-harness.test.ts returns 2 (two tests tagged)
    - npx playwright test --config tests/e2e/playwright/playwright.evaluate.config.ts --grep "@autonomous" --dry-run exits 0 and lists at least 2 tests
    - pnpm type-check passes (no TypeScript errors in new file)
    - grep "request.get\|page.goto" tests/e2e/playwright/eval-harness.test.ts returns matches (both test types present)
  </acceptance_criteria>
  <done>eval-harness.test.ts has two @autonomous tests: one using request fixture for HTTP assertion, one using page fixture for UI navigation; --dry-run discovers both</done>
</task>

<task type="auto">
  <name>Task 2: Write 26-EVAL-CONFIG.yml</name>
  <files>.planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml</files>
  <read_first>
    - .planning/phases/25-loop-architecture-and-evaluator-agent/25-EVAL-CONFIG-SPEC.md (schema reference -- all required fields and criterion type schemas)
    - tests/e2e/playwright/eval-harness.test.ts (get the exact test names for grep_pattern fields)
    - scripts/token-audit.js (verify --quiet flag behavior described in Research is correct)
  </read_first>
  <action>
Write `.planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml` using the EVAL-CONFIG-SPEC.md schema exactly.

This file declares evaluation criteria for Phase 26 (the evaluation harness itself). It also serves as a working reference implementation of the EVAL-CONFIG format for future phases.

Required top-level fields:
- `threshold: 0.85` -- 85% of autonomous criteria must pass
- `max_rounds: 3` -- maximum evaluate-fix cycles
- `server_port: 3099` -- evaluation server port (matches playwright.evaluate.config.ts)
- `seed_scripts:` -- must include the base seed path

Include these criteria in order:

1. `API-01` (type: api_assertion) -- health endpoint check
   - Tests that the server started correctly and a basic endpoint responds
   - Use the same endpoint from eval-harness.test.ts Task 1

2. `UI-01` (type: ui_behavior) -- dashboard renders
   - References `tests/e2e/playwright/eval-harness.test.ts`
   - grep_pattern must match the test name exactly as written in the test file

3. `DS-01` (type: design_token) -- token audit gate
   - `token_audit: true`
   - `files:` list the new files created in Phase 26: `scripts/evaluate/seed-eval.ts` and `tests/e2e/playwright/playwright.evaluate.config.ts` and `tests/e2e/playwright/eval-harness.test.ts`
   - IMPORTANT: The `files:` field for design_token criteria is documentation-only. scripts/token-audit.js does NOT accept file path arguments -- it unconditionally scans SCAN_DIRS. The Phase 27 evaluator MUST invoke it as `node scripts/token-audit.js --quiet` (no file arguments) and check the exit code. Do NOT attempt to pass the files list as CLI arguments.
   - Add this as a YAML comment directly on the `files:` key in the written YAML:
     `# documentation-only: token-audit.js scans SCAN_DIRS unconditionally; Phase 27 evaluator runs 'node scripts/token-audit.js --quiet' with no file arguments`
   - Description: "No hardcoded colors in Phase 26 evaluation harness files"

4. `UNIT-01` (type: unit_test) -- seed determinism
   - Verify seed-eval.ts TypeScript compiles (using tsc --noEmit check)
   - Actually: there are no unit tests for seed-eval.ts. Use a ui_behavior or skip. Instead, add a MANUAL-01.

5. `MANUAL-01` (type: manual_review) -- server lifecycle manual check
   - Description: "Dev server starts cleanly and stops without zombie processes"
   - review_guidance: "Run `npx playwright test --config tests/e2e/playwright/playwright.evaluate.config.ts --grep '@autonomous'` against a running database. Check that the server starts (port 3099 appears in output), tests run, and the server stops after tests complete. Verify no node processes remain with `ps aux | grep 'next-server' | grep -v grep`."

seed_scripts should use the path from the EVAL-CONFIG-SPEC: `tests/e2e/playwright/utils/seed.ts` is the base seed (note: this is the base seed.ts module, not seed-eval.ts which is the globalSetup wrapper). Actually re-read the spec carefully -- the spec says `tests/e2e/playwright/utils/seed.ts` as the base seed. But seed-eval.ts is the Playwright globalSetup. The seed_scripts field in EVAL-CONFIG.yml is read by gsd-evaluator to know what scripts to run -- gsd-evaluator calls these directly (not via globalSetup). So seed_scripts should list the seed-eval.ts wrapper since it does the full cleanup+seed cycle.

Use `scripts/evaluate/seed-eval.ts` as the seed_scripts entry.

Write the file with YAML comments explaining each section for clarity.
  </action>
  <verify>
    <automated>grep -c "threshold:\|max_rounds:\|server_port:\|seed_scripts:\|criteria:" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml</automated>
  </verify>
  <acceptance_criteria>
    - grep "threshold: 0.85" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "max_rounds: 3" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "server_port: 3099" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "seed_scripts" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "type: api_assertion" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "type: ui_behavior" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "type: design_token" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "type: manual_review" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match
    - grep "documentation-only" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml returns a match (YAML comment present on DS-01 files field)
    - Criterion IDs are unique: API-01, UI-01, DS-01, MANUAL-01
  </acceptance_criteria>
  <done>26-EVAL-CONFIG.yml exists with all required top-level fields and at least 4 criteria covering api_assertion, ui_behavior, design_token, and manual_review types; DS-01 files field has YAML comment clarifying it is documentation-only and token-audit.js must be run without file arguments</done>
</task>

</tasks>

<verification>
1. `grep -c "@autonomous" tests/e2e/playwright/eval-harness.test.ts` returns 2
2. `npx playwright test --config tests/e2e/playwright/playwright.evaluate.config.ts --grep "@autonomous" --dry-run` exits 0 and lists 2 tests
3. `grep "threshold: 0.85" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml` matches
4. `node scripts/token-audit.js --quiet` exits 0 (baseline -- no new violations in harness files)
5. `pnpm type-check` exits 0 (no new TypeScript errors)
6. `grep "documentation-only" .planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-EVAL-CONFIG.yml` matches (DS-01 files field comment present)
</verification>

<success_criteria>
- eval-harness.test.ts establishes @autonomous tagging convention with HTTP and UI smoke tests (EVAL-01, EVAL-02 satisfied)
- 26-EVAL-CONFIG.yml references token-audit.js as design_token criterion (EVAL-05 satisfied via DS-01 criterion)
- DS-01 files field is annotated as documentation-only; Phase 27 evaluator contract is clear: run `node scripts/token-audit.js --quiet` without file arguments
- gsd-evaluator can read 26-EVAL-CONFIG.yml and find criteria of each supported type
- pnpm type-check passes
- node scripts/token-audit.js --quiet exits 0 (no new token violations)
</success_criteria>

<output>
After completion, create `.planning/phases/26-dev-server-lifecycle-and-evaluation-harness/26-03-SUMMARY.md`
</output>
