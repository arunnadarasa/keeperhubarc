# CALIBRATION ONLY -- not runnable. Synthetic paths for evaluator regression testing.
# Fixture 05: seed-failure
# Scenario: Seed script exits non-zero. Evaluator must set seed_ran: false and continue evaluation.

threshold: 0.85
max_rounds: 3
server_port: 3099

seed_scripts:
  - scripts/evaluate/fixtures/failing-seed.ts

criteria:
  - id: API-01
    type: api_assertion
    description: "GET /api/health returns 200 even when seed fails"
    method: GET
    url: "/api/health"
    body: null
    auth: "none"
    expected_status: 200
