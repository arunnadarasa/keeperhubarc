# CALIBRATION ONLY -- not runnable. Synthetic paths for evaluator regression testing.
# Fixture 03: convergence-halt
# Scenario: Round 2 failures identical to round 1. Evaluator must write status: convergence_halt.

threshold: 0.85
max_rounds: 3
server_port: 3099

seed_scripts:
  - scripts/evaluate/fixtures/hypothetical-seed.ts

criteria:
  - id: API-01
    type: api_assertion
    description: "GET /api/hypothetical returns 200"
    method: GET
    url: "/api/hypothetical"
    body: null
    auth: "none"
    expected_status: 200

  - id: UI-01
    type: ui_behavior
    description: "Hypothetical page renders title"
    playwright_test: "tests/evaluate/fixtures/hypothetical.test.ts"
    grep_pattern: "renders title @autonomous"
